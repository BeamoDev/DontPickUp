-- Shared workshop tuning; ModeRules owns Story and Endless night progression.
return {
	Enabled = true,
	DebugScares = true,
	DebugScaresInLive = false,
	ScareDamage = 25,
	ScareImpactDelay = 0.6,
	OutsideRisk = {
		Enabled = true,
		GraceSeconds = 12,
		RollInterval = 8,
		BaseChance = 12,
		ChancePerSecond = 0.8,
		MaxChance = 65,
		WarningSeconds = 7,
		Cooldown = 30,
		Damage = 35,
		LethalDamage = 100,
	},
	Director = {
		Enabled = true,
		CalmSeconds = 90,
		FirstJitter = 15,
		IntervalMin = 28,
		IntervalMax = 52,
		QuietChance = 25,
		MaxEvents = 5,
		MaxScares = 2,
		ScareChance = 45,
		KindCooldown = 85,
		LethalAfter = 180,
	},
	NightSeconds = 360,
	ArrivalSeconds = 4,
	CustomerPatience = 105,
	BetweenCustomers = 9,
	WorkSeconds = 2.5,
	FittingSeconds = 1.2,
	PuzzleTimeout = 120,
	SecureContacts = 4,
	VoteSeconds = 18,
	InteractionDistance = 13,
	AfterDisturbanceSeconds = 6,
	Customers = require(script.Parent.Parent.Customers.CustomerCatalog).Customers,
	-- Fixed scenarios remain available for regression tests or Director.Enabled = false.
	Events = {
		{
			At = 55,
			Kind = "Blackout",
			Duration = 25,
			Warning = "POWER FAILURE. Restore the fuse box before the backup light dies.",
		},
		{
			At = 130,
			Kind = "Call",
			Duration = 22,
			Warning = "The disconnected phone is ringing. Silence it. Do not answer.",
		},
		{
			At = 215,
			Kind = "Inspection",
			Duration = 25,
			Warning = "An inspector is outside. Present the repair records at the filing cabinet.",
		},
		{
			At = 305,
			Kind = "Visitor",
			Duration = 20,
			Warning = "Someone is at the window. Stay in the marked storage shelter until it leaves.",
		},
	},
	Rules = "NIGHT DIRECTIVE 01\n1. Repair and return customer devices.\n2. Install the government package on every repair.\n3. Report suspicious files; your team can choose otherwise.\n4. Silence the disconnected phone. Do not answer.\n5. When someone watches the window, shelter in storage.",
}
